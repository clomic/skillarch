|Function/Alias|Description|
|---|---|
|10to16|Convert Int to Hex (Base 10 → Base 16 - 15926488642753 → E7C2CB2B4C1)|
|10to2|Convert Int to Bin (Base 10 → Base 2 - 3797170 → 1110011111000010110010)|
|16to10|Convert Hex to Int (Base 16 → Base 10 - E7C2CB2B4C1 → 15926488642753)|
|2to10|Convert Bin to Int (Base 2 → Base 10 - 1110011111000010110010 → 3797170)|
|args_to_stdin|Convert Args to stdin|
|b|bat (A cat(1) clone with syntax highlighting and Git integration)|
|b64|Encode into Base64|
|b64d|Decode from Base64|
|b64e|Encode into Base64|
|b64url|Encode into Base64 with URL compliant|
|b64urld|Decode from Base64 URL safe|
|b64urle|Encode into Base64 URL safe|
|back-n|Convert `\n` into newline|
|back-t|Convert `\t` into tab|
|base10to16|Convert Int to Hex (Base 10 → Base 16 - 15926488642753 → E7C2CB2B4C1)|
|base10to2|Convert Int to Bin (Base 10 → Base 2 - 3797170 → 1110011111000010110010)|
|base16to10|Convert Hex to Int (Base 16 → Base 10 - E7C2CB2B4C1 → 15926488642753)|
|base2to10|Convert Bin to Int (Base 2 → Base 10 - 1110011111000010110010 → 3797170)|
|batall|Show all files (sub-directories included) into bat|
|c|Open Visual Studio Code|
|C|Open current folder in Visual Studio Code |
|cdtmp|Create a Temp Directory and go into it|
|cfu|Parse ffuf JSON: status/length/lines/words/url|
|cfu-clean|Deduplicated cfu output|
|cfu-clean-url|URLs only from cfu-clean|
|cgrep|grep with color|
|cheat|`curl cheat.sh/<arg>`|
|clean-swap|Clean /proc/swaps|
|cmd_picker|Allow to pick a custom command or alias|
|cp|cp with -i (prompt before overwrite)|
|cpy|Copy stdin to Clipboard (xclip)|
|crl|curl with real browser UA|
|crli|crl + dump response headers to stderr|
|crlix|crli proxied through Caido (127.0.0.1:8080)|
|da|`cd /DATA`|
|dbinwalk-dd|binwalk extract all file types (`binwalk --dd='.*'`)|
|dcarbonyl|Chrome in Terminal|
|ddroopescan|droope/droopescan \| Drupal scanner|
|dex|fzf-select container + exec into it|
|dexr|fzf-select container + exec as root|
|dgitleaks|zricethezav/gitleaks \| Secret scanning|
|dhashcat|dizcza/docker-hashcat \| CPU/GPU hash cracking|
|di|Inspect container (fzf)|
|didi|Launch a fresh updated discord|
|digall|`dig +answer +multiline <domain> any @8.8.8.8`|
|dip|Inspect container (fzf) and grep IPs |
|dirmon|`inotifywait` recursive file monitoring|
|djohn|phocean/john_the_ripper_jumbo \| Password cracking|
|djwt-tool|ticarpi/jwt_tool \| JWT testing|
|dkillall|Disable restart + stop all|
|dl|`cd ~/Downloads`|
|dmsfconsole|metasploitframework \| Metasploit|
|dmsfvenom|metasploitframework \| Metasploit|
|dnorestart|`docker update --restart=no $(docker ps -q)`|
|dns-1|Switch DNS to 1.1.1.1|
|dns-127|Switch DNS to 127.0.0.1|
|dns-8|Switch DNS to 8.8.8.8|
|dns-9|Switch DNS to 9.9.9.9|
|dockit|Run any image with cwd mounted|
|dockns|nsenter into container's namespaces|
|doneforall|shmilylty/oneforall \| Comprehensive recon|
|dphp7|PHP 7.4 docker image|
|dps|`docker ps`|
|drecon-amass|caffix/amass \| Subdomain enumeration|
|drecon-findomain| edu4rdshl/findomain \| Fast subdomain discovery|
|drecon-wappa|wappalyzer/cli \| Tech fingerprinting|
|dretdec|blacktop/retdec \| Binary → C decompiler|
|drustscan|rustscan/rustscan \| Fast port scanner|
|dsecator|freelabz/secator \| Normalize offensive arsenal|
|dssh-audit|positronsecurity/ssh-audit \| SSH config audit|
|dssl-test|drwetter/testssl.sh \| TLS/SSL testing|
|dstopall|Stop all containers|
|dswagger-editor|Launch on port 8000 a swagger editor (swaggerapi Docker image)|
|dsysdig|sysdig/sysdig \| System-level introspection|
|dtrufflehog|trufflesecurity/trufflehog \| Secret scanning in git history|
|dwipe-all|Full docker system prune|
|dwipe-image|Wipe all Docker images|
|dwipe-network|Wipe all Docker network interfaces|
|dwipe-process|Wipe all Docker processes|
|dwipe-volume|Wipe all Docker volumes|
|dwpscan|wpscanteam/wpscan \| WordPress scanner|
|fab|`fabric-ai -s` (AI-powered text processing)|
|fastfetch|fastfetch with SkillArch logo|
|ff|`find . -iname "*<arg>*"` |
|flameshotz|Take a full screen screenshot every second until break (save into ~/Downloads)|
|fromDec|Convert from ordinal to ASCII (e.g. fromDecimal "83 107 105 108 108 65 114 99 104" → SkillArch)|
|fromDecimal|Convert from ordinal to ASCII (e.g. fromDecimal "83 107 105 108 108 65 114 99 104" → SkillArch)|
|fromEpoch|Convert Epoch Time into UTC Timestamp (e.g. `fromEpoch "1778605690"` → 2026-05-12T17:08:10+00:00)|
|fromHex|`fromHex 536b696c6c41726368` → SkillArch|
|fromUnicode|`fromUnicode "U+1f680 \u1F339 %u1f499"` → 🚀 🌹 💙|
|fu|ffuf with JSON output, browser UA, `-mc all`|
|fzf-h|fzf of history|
|fzf-w|fzf of Wordlists|
|gdh|`git diff HEAD`|
|get-alpha|Get alpha characters ([A-Za-z])|
|get-alphanum|Get alphanum characters ([0-9A-Za-z])|
|get-badchars|Print common injection characters|
|get-bytes-hex|All 256 bytes in hex form|
|get-bytes-raw|All 256 bytes in raw form|
|get-bytes-url|All 256 bytes in URL form|
|get-chars|Print all characters from char 32 (space) to char 126 (~)|
|get-du|`du -hd1 2>/dev/null`|
|get-homoglyph|Give the list of homoglyph for characters (e.g. `get-homoglyph '*'\|tr -d '\n'` → ⁎٭∗𐌟)|
|get-ip|Public IP via ipinfo.io|
|get-ip-cpy|Public IP address is copied into the Clipboard and not displayed|
|get-meteo|`curl wttr.in` weather|
|get-pid-click|Click a window to get its PID|
|get-pid-ps|fzf-select process to get PID|
|get-shell-size|Get numbers of rows and cols in current shell|
|getcan|Generate a Canary with the current unix timestamp in nanosecond (e.g. skanary1778607587707828891)|
|getcaninfo|get time from canary (`getcaninfo skanary1778607587707828891` or `getcaninfo 1778607587707828891` → 2026-05-12 19:39:47.707828891) |
|getfuzz|``AA<fuzz1>"fuzz2'fuzz3`%}})fuzz4${{fuzz5\BB``|
|getinfo-bookhacktricks|Search book.hacktricks.wiki (opens browser)|
|getinfo-certspotter|CT log lookup via certspotter API|
|getinfo-crtsh|crt.sh JSON certificate search|
|getinfo-known-creds|Open cirt.net for default creds|
|getinfo-known-port|Open speedguide.net for port info|
|getinfo-leakix|LeakIX hostname/IP graph lookup|
|getinfo-virustotal|Open VirusTotal for domain|
|getinfo-wayback|Wayback Machine URL enumeration|
|git-clone-all-github|Clone all repos from `$GITHUB_USER` via API|
|git-clone-all-gitlab|Clone all owned GitLab repos|
|git-pull-all|`git pull` in all subdirs (parallel)|
|grephilight|Change the Highlight of grep colors|
|hex2c|Convert Hex to C hex string (e.g. `hex2c 909090` → \x90\x90\x90)|
|hex2py|Convert Hex into a Python hex array (e.g. `hex2py 909090` → [0x90, 0x90, 0x90])|
|hexd|Decode Hex to ASCII string (e.g. `hexe 536b696c6c41726368` → SkillArch)|
|hexdecode|Decode Hex to ASCII string (e.g. `hexe 536b696c6c41726368` → SkillArch)|
|hexe|Encode string to Hex (e.g. `hexe SkillArch` → 536b696c6c41726368)|
|hexencode|Encode string to Hex (e.g. `hexe SkillArch` → 536b696c6c41726368)|
|hifetch|fastfetch with SkillArch logo|
|htmlencode|Convert characters to HTML Entities (e.g. `&` → `&amp;`)|
|htmlunescape|Convert HTML Entities back to characters (e.g. `&amp;` → `&`)|
|ipa|`ip -c -br a` (Only UP)|
|ipaa|`ip -c -br a` (ALL)|
|ipv6-disable|Disable IPv6 via sysctl|
|ipv6-enable|Enable IPv6 via sysctl|
|jsonescape|`jsonescape '{"distrib": "SkillArch", "quote": "That'"'"'s awesome!"}'` → {\"distrib\": \"SkillArch\", \"quote\": \"That's awesome!\"}|
|jsonunescape|`jsonunescape '{\"distrib\": \"SkillArch\", \"quote\": \"That'"'"'s awesome!\"}'` → {"distrib": "SkillArch", "quote": "That's awesome!"}|
|killit|`sudo kill -KILL` |
|l|`eza -l --group-directories-first`|
|la|`l -a`|
|latinToUtf8|`fromHex 48e920e761207661203f\|latinToUtf8` → Hé ça va ? |
|lfu|ffuf + save JSON + auto-filter results|
|lgrep|`grep --line-buffered` (flush output on every line)|
|makeqrcode|QR code in terminal|
|mcrypt-dec|Decrypt mcrypt file (`mdecrypt`)|
|mcrypt-enc|Encrypt file with a random password (auto-copied to clipboard)|
|md5|MD5 Hash|
|nc|`ncat`|
|ncl|`ncat -lnvp` (listen)|
|neofetch|fastfetch with SkillArch logo|
|nnb|Strip null bytes|
|nocolor|Strip ANSI codes from stdin|
|nonullbyte|Strip null bytes|
|nosleep|Prevent system sleep (systemd-inhibit)|
|nthash|NtHash in lowercase|
|NThash|NtHash in Uppercase|
|p|python|
|paste|Paste from clipboard|
|probe-urls|Bulk HTTP probe from URL list file|
|pub|Export `$PUB` as the public IP address, copy it into the terminal and the clipboard|
|pv|`pv 3.12` Create mise-managed Python venv (version arg required)|
|pyreq|`pip install -r requirements.txt`|
|pysrv|HTTP server from empty /tmp dir|
|rm|`trash-put` (safe delete)|
|sdi|`watch iostat -h` (Show Disk IO)|
|sha1|SHA1 Hash|
|sha256|SHA256 Hash|
|show-disk-io|`watch iostat -h`|
|show-open-ports|Show TCP Open ports (Listen mode)|
|ska|`cd /opt/skillarch`|
|ska-help-aliases|fzf fuzzy-search all aliases|
|ska-help-bindings|fzf fuzzy-search i3 keybindings|
|ska-help-packages|fzf fuzzy-search installed pacman packages|
|ska-sudo-unlock|Reset faillock after 3 bad sudo attempts|
|ska-update-advanced|Interactive upstream-merge for forks (commit local → fetch → preview → merge → push → reapply, all Y/n-prompted)|
|ska-update-simple|`ska && make update && make install`|
|ska-vbox-install|Install Virtualbox package|
|ska-vbox-install-guestutils|Install VirtualBox guest utils|
|ska-vnc|Start KasmVNC on :1 (no auth, localhost only)|
|skao|`cd /opt/skillarch-original`|
|sop|Show TCP Open ports (Listen mode)|
|sortn|`sort -V \| uniq -c \| sort -n`|
|ssh-yolo|SSH ignoring host key checks|
|suc|`sort\|uniq -c`|
|suq|`sort\|uniq`|
|t|eza tree|
|t2|eza tree depth 2|
|t3|eza tree depth 3|
|ta|tmux attach|
|te|trash-empty|
|tn|tmux new|
|toDec|Convert into ordinal int array (e.g. `toDec "SkillArch"` → 83 107 105 108 108 65 114 99 104)|
|toDecimal|Convert into ordinal int array (e.g. `toDecimal "SkillArch"` → 83 107 105 108 108 65 114 99 104)|
|toHex|(e.g. `toHex "SkillArch"` → 536b696c6c41726368)|
|toLower|All in lower case|
|toUnicode|`toUnicode "Windows 🪟 vs Linux 🐧"` → Windows \u1fa9f vs Linux \u1f427|
|toUpper|All in Upper case|
|unicodedecode|`unicodedecode "SkillArch\u1f680"` → SkillArch🚀|
|unicodeencode|`unicodeencode "Windows 🪟 vs Linux 🐧"` → Windows \u1fa9f vs Linux \u1f427|
|unicodeencodeall|`unicodeencodeall 'SkillArch🚀'` → \u0053\u006b\u0069\u006c\u006c\u0041\u0072\u0063\u0068\u1f680|
|upload|Upload file to 0x0.st|
|urld|URL decode (e.g. `urld "%53%6b%69%6c%6c%41%72%63%68"` → SkillArch)|
|urldecode|URL decode (e.g. `urldecode "%53%6b%69%6c%6c%41%72%63%68"` → SkillArch)|
|urle|URL encode (partial) (e.g. `urle 'SkillArch rocks!'` → SkillArch%20rocks%21)|
|urleall|URL encode (full) (e.g. `urleall 'SkillArch'` → %53%6b%69%6c%6c%41%72%63%68)|
|urlencode|URL encode (partial) (e.g. `urlencode 'SkillArch rocks!'` → SkillArch%20rocks%21)|
|urlencodeall|URL encode (full) (e.g. `urlencodeall 'SkillArch'` → %53%6b%69%6c%6c%41%72%63%68)|
|usleep|Sleep in floating seconds (e.g. `usleep 1.5`)|
|utf16|Encode in UTF-16 little-endian|
|utf16be|Encode in UTF-16 Big-endian|
|utf16le|Encode in UTF-16 little-endian|
|utf16to8|Encode UTF16-LE to UTF-8|
|utf8toLatin|Convert UTF-8 to Latin-1|
|v|nvim|
|webcam-guvcview|guvcview|
|wget-yolo|`wget --no-check-certificate`|
|wifi-nmtui|Text User Interface for controlling NetworkManager (Wifi)|
|xh|xxd <last_arg> \| head <args_but_last>  (`xh -n -20 file` == `xxd file \| head -n -20 file`)|
|xmlescape|Convert characters to HTML Entities (e.g. `&` → `&amp;`)|
|xmlunescape|Convert HTML Entities back to characters (e.g. `&amp;` → `&`)|
|xor|`xor "my_key" "my_string"` or  `echo "my_string" \| xor "my_key"`|
|xorh|`xorh "key_in_hex" "hex_string"` or  `echo "hex_string" \| xorh "key_in_hex"`|
|yt-dlp-likes|Download Youtube Favorites Videos to MP3|
|yt-mp3|Extract audio (in MP3) from Youtube link|
